/* In questo esempio implementiamo l'algoritmo di euclide */
(do
    (read a)
    (read b)

    (while (<> a b)
        (cond (> a b) (= a (- a b)) (else (= b (- b a))))
    )
    (print a) // Risultato
)